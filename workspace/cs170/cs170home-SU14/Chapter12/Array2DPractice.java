import java.util.Scanner;
import acm.program.*;
/**
 * Array2D practice exercises for CS 170.
 * @author Stephen Gilbert
 * @version Summer 2014
 */
public class Array2DPractice extends ConsoleProgram
{
    /**
     * EXERCISE 1: average a 2D array of int.
     * @param a the 2D int array to average.
     * @return the 2D int average of the elements in the array.
     */

    /**
     * EXERCISE 2: average a 1D array of int.
     * @param a the 1D int array to average.
     * @return an int average.
     */


    /**
     * EXERCISE 3: average the element of one column of a 2D array of int.
     * @param a the 2D int array to average.
     * @param col the column to average
     * @return an int average.
     */


    /**
     * Process a 2D array and the rows in a 2D array.
     */
    public void run()
    {
        // Write the average() method
        int[][] values = { {2, 5}, {6, 7}, {3, 9} };

        // EXERCISE 1:  Write a method to average a 2D array.
        //              Name the method average() and return
        //              the int (not double) average
        //  When finished, remove the comment on the line below and run.
//        println("The average in values is " +
//                average(values));


        // EXERCISE 2:  Write a method to average a 1D array.
        //              Name the method average() and return
        //              the int (not double) average
        //  When finished, remove the comment on the line below and
        //  pass the SECOND ROW of the values array to the method.
//        println("The average of second row of values is " +
//            average( /* fill this in */ ));

        // EXERCISE 3:  Write a third method named average().
        //              This version should return the int average
        //              of a single COLUMN in the array.
        //  When finished, remove the comment on the line below and
        //  pass the correct parameters so the SECOND COLUMN average is printed.
//        println("The average of second column of values is " +
//            average( /* fill this in */ ));

    }


}
